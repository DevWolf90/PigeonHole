<% pigeons.each do |pigeon| %>
  <% if card_filter.call(pigeon) %>
    <%= render 'pigeons/pigeon_card', locals: { pigeon: pigeon, card_class: card_class, pigeon_avatar: pigeon_avatar, media_icon: media_icon, particular: particular } %>
  <% end %>
<% end %>
